#include <limits>
#include <iostream>
#include <map>
#include <vector> 
/*template <typename T>
struct TypeOf
{
  typedef T type;
};

template <typename T>
TypeOf<T> type_of( const T& v )
{
  return TypeOf<T>();
}

int main(){
int a;
std::cout << "Size : " << std::numeric_limits<type_of(a)::type>::max() << std::endl;
std::cout << "Size : " << std::numeric_limits<int>::max() << std::endl;

return 0;
}


*/

template<typename T>
class MyType{
std::map<std::string,T> fMap;

};


class Type{

public:

template<typename T>
    void CreateBranch(std::str)
   
};

int main(){

    MyType<int> mt;
    Type t;
    //t.Print<decltype(mt)>();

    return 0;
}
